// Deprecated placeholder kept empty intentionally after splitting sync into
// `external-refs` and `sync-states`. The active generated resources are:
// - task-external-ref.resource.ts
// - task-sync-state.resource.ts
export {};
